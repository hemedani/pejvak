# Copy to `.env` and adjust. Deno does not auto-load `.env`; either export these
# in your shell or run with `deno run --env-file=.env ...`.
MONGO_URI=mongodb://127.0.0.1:27017/
DB_NAME=pejvak
APP_PORT=1405
ENV=development
TOKEN_KEY=pejvak_dev_secret_change_me
